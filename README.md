# cfmpeg Homebrew Tap

Homebrew formulae for cfmpeg.

Install cfmpeg with:

```bash
brew tap cfmpeg/cfmpeg-homebrew-tap https://github.com/cfmpeg/cfmpeg-homebrew-tap.git
brew install cfmpeg/cfmpeg-homebrew-tap/cfmpeg
```

